{
  "schema_version": "runner-contract/v1",
  "source_files": ["lib/agent-functions.sh", "lib/monitor-completion.sh"],
  "role": "monitor pty agent sessions, latch completion, classify stale/dead, and launch completion handler",
  "completion_latch": {
    "rule": "OR latch explicit terminal marker or declared emits event",
    "terminal_marker": "AGENT_COMPLETE on its own stripped terminal line",
    "event_match": "expected emits event with source containing agent id and matching run id",
    "latch_file": "~/.mentiko_monitor/${session}_complete"
  },
  "stale_behavior": {
    "detector": "md5 of last 20 captured lines unchanged",
    "advisor_threshold": "MENTIKO_ADVISOR_STALE_COUNT default 3",
    "max_stale": "MENTIKO_MONITOR_MAX_STALE or DEFAULT_MAX_STALE_COUNT default 5",
    "effect": "advisor nudge, blocked diagnostic, or failure classification",
    "nudge_budget": "durable ceiling MENTIKO_MONITOR_MAX_NUDGES tracked in ${session}_nudges; survives screen-echo stale resets and monitor restarts; an exhausted budget escalates instead of nudging forever (20ce3b1)",
    "never": "stale output does not imply completion"
  },
  "death_behavior": {
    "local": "pgrep child process under pane pid",
    "remote": "skip local process death checks for ssh/docker",
    "with_event": "complete normally",
    "without_event": "failed diagnostic agent-error"
  },
  "launches": {
    "completion": "chain-runner-complete in separate pty when chain.json exists",
    "typed_bridge": "when MENTIKO_RUNNER_V2 and MENTIKO_RUNNER_V2_COMPLETION are enabled, the completion pty runs node runner-v2-complete.js first; exit != 64 is terminal (typed pipeline handled completion), exit 64 execs the shell completion script (20ce3b1)",
    "completion_env": "completion session carries MENTIKO_RUN_DIR ($RUNS_DIR/$run_id), EVENTS_DIR, STATE_DIR, both runner-v2 flags, and MENTIKO_CODE_ROOT anchored from the engine location (20ce3b1, 5e79030, 6504637)",
    "legacy": "complete-agent.sh only when monitor has no chain.json"
  },
  "invariants": [
    "stale is not complete",
    "dead without declared event is not success",
    "completion matcher never accepts monitor diagnostic events",
    "AGENT_COMPLETE must be standalone line",
    "event match respects agent id and run id",
    "completion handler runs outside monitor pty",
    "screen-echo repaints never grant more nudges: the nudge budget is durable (20ce3b1)",
    "exported-fn bodies avoid heredocs (export -f serialization hazard, 0c96b73)"
  ]
}
