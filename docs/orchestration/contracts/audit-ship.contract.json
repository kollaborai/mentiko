{
  "schema_version": "audit-ship/v1",
  "owner": "web/lib/runner-v2/audit-ship.ts",
  "cli": "web/lib/runner-v2/audit-ship-cli.ts",
  "compiled": "lib/runner-audit-ship.js",
  "owns": [
    "raw JSONL audit-entry decoding and object validation",
    "normalized audit identity and timestamp validation",
    "namespace substitution, S3 URL parsing, and remote key derivation",
    "rclone retry orchestration and typed failure-breadcrumb serialization"
  ],
  "invariants": [
    "malformed raw input fails closed and never becomes an invented audit object",
    "the audit writer invokes the compiled owner directly with stdin and never parses or serializes remote-ship JSON",
    "rclone remains the required external object-storage CLI",
    "remote shipping failures append a durable failure breadcrumb but never block the audit writer"
  ],
  "external_processes": {
    "rclone": "rclone copyto <local-entry> :s3:<bucket>/<remote-key>"
  },
  "entrypoint": "web/lib/system/audit-log.ts launches lib/runner-audit-ship.js directly"
}
