{
  "schema_version": "runner-contract/v1",
  "generated_at": "2026-07-04",
  "migration_mode": "side-by-side",
  "default_runner": "shell",
  "flag": {
    "name": "MENTIKO_RUNNER_V2",
    "enabled_values": [
      "1",
      "true",
      "yes",
      "on"
    ],
    "default": "off",
    "scope": "initial web chain launch; completion re-entry requires the separate completion flag"
  },
  "completion_flag": {
    "name": "MENTIKO_RUNNER_V2_COMPLETION",
    "enabled_values": [
      "1",
      "true",
      "yes",
      "on"
    ],
    "default": "off",
    "scope": "opt-in typed completion re-entry bridge from monitor handoff"
  },
  "entrypoints": {
    "web_initial_launch": {
      "current": "web/lib/runs/chain-run-service.ts -> /bin/zsh -lc 'bin/mentiko run <chain.json>'",
      "v2": "web/lib/runner-v2/controller.ts -> web/lib/runner-v2/agent-bootstrap-plan.ts validates first-agent/profile context, then typed-plan still delegates PTY startup to lib/chain-runner.sh --start",
      "fallback": "shell launch when v2 is disabled or when v2 returns unsupported before typed bootstrap side effects; do not shell-fallback after v2 allocates sessions or mutates run state"
    },
    "completion_reentry": {
      "current": "lib/chain-runner-complete.sh -> lib/chain-runner.sh --start/--parallel",
      "v2": "lib/agent-functions.sh -> compiled /opt/mentiko/lib/runner-v2-complete.js when MENTIKO_RUNNER_V2_COMPLETION is enabled",
      "fallback": "shell completion when completion flag is disabled or typed bridge exits unsupported (exit 64); the bridge exits unsupported for members of a live fan group because typed fan-in accounting is not wired — the shell handler owns those completions",
      "routed_coverage": "agents relaunched by the typed bridge (routed chain-runner --start/--parallel) complete through the typed pipeline too: shell chain-runner monitors carry the completion flags + run-dir context inline (pty daemon whitelists spawn env), and the completion entrypoint adopts an AgentAttempt record (origin routed-completion-adoption, created at instructions_submitted from completion-time evidence) before the verdict drives the normal completed/completion_failed edges",
      "division_of_labor": "exactly one handler applies completion side effects: typed bridge exit != 64 means the shell handler never runs (typed queues external effects to the outbox); exit 64 means the shell handler applies everything inline",
      "reason": "completion owns fan-in, fan-out, retries, generation backstop, and event archiving; typed re-entry is opt-in until full branch parity is proven"
    },
    "daemon_watchers": {
      "current": "chain-runner ensures mentiko-watchdog and mentiko-chain-watcher pty sessions",
      "v2": "must preserve singleton behavior before replacing shell startup"
    }
  },
  "disk_contracts": {
    "run_json": {
      "path": "$RUNS_DIR/<run_id>/run.json",
      "lock": "<run.json>.lock/",
      "write_policy": "read-modify-write under lock, temp file, atomic rename",
      "read_policy": "lock-free readers tolerate old or new whole file",
      "run_success_status": "completed",
      "agent_success_status": "complete",
      "initial_status": "pending in shell create-run; web-created runs may already be running or pending"
    },
    "event_file": {
      "format": "key-value text",
      "fields": [
        "event",
        "source",
        "run_id",
        "timestamp",
        "processed",
        "data"
      ],
      "parser_rule": "split on first colon; data may contain more colons",
      "processed_default": "false",
      "source_of_truth": "body fields before filename fallback"
    }
  },
  "generation_completion_contract": {
    "core_generation_handoff": "generation-result.json is authoritative; mentiko emit is optional for core generation chains",
    "no_emit_salvage": "typed completion must import a compatible generation payload before failing a core generation agent that reached completion without its declared event",
    "import_effect": "typed completion executor must include a generation-import effect that invokes the existing mentiko generation import path",
    "prompt_policy": "core generation bootstrap instructions must not require mentiko emit as a mandatory final step"
  },
  "external_effects": {
    "outbox": "typed completion adapters queue external side effects to <stateDir>/external-effects.jsonl (STATE_DIR when the engine exports it, else <runDir>/state); records carry namespaceId/orgId",
    "dispatcher": "web/lib/runner-v2/external-effects.ts",
    "live_drain": "web/server/background-worker.ts drains outboxes every 15s via drainRunnerV2ExternalEffects: claim-by-rename consumption, per-record tenant identity, bounded redelivery (3 attempts), audit to external-effects.dispatch.jsonl",
    "handled_operations": [
      "notification",
      "webhook",
      "metadata-webhooks",
      "task-status",
      "plugin",
      "legacy-webhook"
    ],
    "plugin_dispatch": "delegates to lib/plugin-runner.sh run-plugins so plugin behavior cannot drift from the shell runner; live-drain opt-in only \u2014 probe and proof dispatch never invoke real plugins",
    "legacy_webhook_network": "live drain delivers legacy webhooks over the network (SSRF guards live in outbound-webhook-delivery); probe dispatch requires explicit opt-in",
    "plain_fail_steps": "plain fail decisions (agent failed, no retry policy) emit terminal-failure steps mirroring the shell no-event failure path: task-status failed, circuit-breaker record-failure, agent-failed notification, failed metadata-webhooks; the shell fires no plugins on this path so neither does typed",
    "agent_completion_steps": "every typed completion that marks the agent complete (route, loop-complete, max-rounds-stop, terminal, generation-terminal) queues per-agent effects mirroring the top of chain-runner-complete.sh: agent-completed plugin, agent-completed notification (agent_complete type, never chain-level), and chain-config agent_complete webhooks (config.webhooks enabled + subscribed, queued as legacy-webhook records per url); unlike the shell, typed does not fire agent-completed for verdicts that fail the agent",
    "no_downstream_terminal": "a completion whose event has no downstream work plans the full terminal completion (task-status, chain_complete webhook, chain-complete event, plugins, notifications, run-completed hooks, metadata-webhooks, legacy webhook, on_complete policy) instead of only flipping run status; a wait with downstream still in flight (targets already active or blocked on prerequisites) applies no run-terminal effects at all, matching the shell's quiet exit"
  },
  "monitor_contract": {
    "completion": "OR-latched explicit AGENT_COMPLETE line or declared emits event match",
    "stale": "stale/idle only drives advisor nudges or diagnostics",
    "env_carry": "the pty daemon whitelists spawn env, so every monitor spawner must inline-export the runner-v2 completion context (MENTIKO_RUNNER_V2, MENTIKO_RUNNER_V2_COMPLETION, MENTIKO_RUN_DIR/RUNS_DIR, NAMESPACE_ID/ORG_ID, EVENTS_DIR/STATE_DIR) into the monitor command; both spawners comply — the typed bootstrap (agent-bootstrap-plan runContextExports) and the shell chain-runner mon_script — otherwise routed completions cannot reach the typed bridge",
    "forbidden": [
      "idle implies complete",
      "dead without declared event implies success",
      "monitor diagnostic event counts as handoff success"
    ],
    "event_match": {
      "agent_id": "MENTIKO_AGENT_ID or session-name inference",
      "event": "agent emits field lower-case equals event file event lower-case",
      "source": "event source contains agent id case-insensitive",
      "run_id": "if present in env, event run id must match"
    }
  },
  "watcher_contract": {
    "event_trigger_source": "chain.json config.event_triggers",
    "enabled": "trigger included unless enabled == false",
    "idempotency": "handled marker under runtime/chain-watcher/handled",
    "condition_policy": "fail closed on rejected shell metacharacters or malformed condition",
    "launch_env": [
      "NAMESPACE_ID",
      "CHAIN_TRIGGER_EVENT",
      "CHAIN_TRIGGER_SOURCE",
      "CHAIN_INPUT"
    ]
  },
  "watchdog_contract": {
    "alive_source": "live PTY session wins over age/stale checks",
    "foreign_scope": "never reap sessions from another root/namespace/org",
    "run_stop": "use shared run lock when available",
    "stalled_event": "diagnostic run-stalled event is not canonical agent handoff"
  },
  "invariants": [
    "do not mutate existing shell runner behavior when MENTIKO_RUNNER_V2 is unset",
    "do not use stale json schemas as the runtime truth",
    "do not collapse watcher/watchdog/scheduler/completion into one implicit state machine",
    "do not fabricate agent success events",
    "do not archive sibling-agent or other-run events",
    "preserve run.json lock interoperability with bash",
    "preserve agent terminal status complete vs run status completed",
    "preserve chain-authored trigger configuration",
    "preserve namespace/org/root scoping for daemon sessions",
    "completion re-entry remains shell fallback-capable until parity tests cover every branch"
  ],
  "known_gaps": [
    "fan-out member completions are intentionally shell-handled: the typed bridge exits unsupported (64) for members of a live fan group because typed fan-in accounting is not wired to the completion entrypoint; wiring the fan-group store into typed completion removes the last routed-coverage carve-out",
    "task update on run completion refreshes last_run_status/timestamps but leaves last_run_agents and last_run_artifacts stale from the prior run until the run-summary pipeline rewrites them (observed live on TASK-173, 2026-07-04)",
    "completed agents' PTY sessions can linger after typed completion handles their AGENT_COMPLETE (writer start script still alive minutes later; cleanup only kills the monitor session)"
  ]
}
