{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://mentiko.dev/schemas/event.schema.json",
  "title": "Parsed Runner Event",
  "description": "Normalized record returned by web/lib/runner-v2/events.ts after reading a physical key-value .event file. Event names are intentionally open because chains define their own handoff vocabulary.",
  "type": "object",
  "required": ["event", "source", "runId", "timestamp", "processed", "data", "fields"],
  "additionalProperties": false,
  "properties": {
    "event": {
      "type": "string",
      "minLength": 1,
      "description": "System event name or chain-defined handoff event."
    },
    "source": {
      "type": "string",
      "minLength": 1,
      "description": "Agent, session, or system source copied from the required physical source field."
    },
    "runId": {
      "type": "string",
      "description": "Normalized from the required physical run_id field; the value may be empty for an unscoped event."
    },
    "timestamp": {
      "type": "string",
      "minLength": 1,
      "description": "Parseable timestamp copied from the required physical timestamp field."
    },
    "processed": {
      "type": "boolean"
    },
    "data": {
      "type": "string"
    },
    "fields": {
      "type": "object",
      "additionalProperties": { "type": "string" },
      "description": "All first-occurrence physical key-value fields, normalized to lowercase keys."
    },
    "path": {
      "type": "string"
    }
  },
  "definitions": {
    "canonicalSystemEventType": {
      "type": "string",
      "description": "Known platform lifecycle vocabulary. This is documentation, not a closed event-name gate.",
      "enum": [
        "manual-start",
        "chain-started",
        "chain-complete",
        "chain-error",
        "agent-started",
        "agent-complete",
        "agent-error",
        "agent-timeout",
        "webhook-triggered",
        "schedule-triggered",
        "fan-in-complete",
        "fan-out-complete",
        "run-stalled",
        "task-status-updated"
      ]
    }
  }
}
